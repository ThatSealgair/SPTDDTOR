#ifndef binary-search.h
#define binary-search.h

#include <stddef.h>

int binary_search_iter(int want, int arr[], int min, int max);

int binary_search_rec(int want, int arr[], int min, int max);

#endif
